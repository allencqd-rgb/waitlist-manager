# 2026-05-25 工作日志

### 09:25  -  UI优化与美化

- 界面美化与交互优化
- 修复工号太长导致的布局溢出问题
- 更新原型文件UI为最新设计风格

### 11:27  -  部署与PRD整理

- 部署局域网环境供同事体验，访问地址 http://192.168.3.6:5173/
- 启动产品经理子代理重新梳理PRD
- 将PRD同步到飞书，命名为「演出候补递补管理系统PRD」
# 2026-05-25 工作日志

### 09:25  -  UI优化与美化

- 界面美化与交互优化
- 修复工号太长导致的布局溢出问题
- 更新原型文件UI为最新设计风格

### 11:27  -  部署与PRD整理

- 部署局域网环境供同事体验，访问地址 http://192.168.3.6:5173/
- 启动产品经理子代理重新梳理PRD
- 将PRD同步到飞书，命名为「演出候补递补管理系统PRD」

### 11:58  -  剧目删除后卡片消失Bug修复

- 问题：蓝色区域右键删除剧目后，右侧剧目卡片也一并消失
- 根因：`allShowCombinations` 纯从 `entries` 计算，候补记录删光后剧目从列表中消失
- 修复：让 `allShowCombinations` 合并 `deletedShows` 中的剧目，确保已删除剧目卡片始终可见（红色边框+半透明+已删除标签）

### 12:00  -  多选批量拖拽递补功能

- 候补名单表格工号前新增多选框，支持单选/全选
- 选中行高亮蓝色背景，拖拽选中行时携带所有选中记录
- 多条拖拽显示自定义拖拽图像「X 条记录」

### 12:04  -  批量递补只成功1条Bug修复

- 问题：拖拽10条记录到剧目卡片，只有1条成功递补
- 根因：循环中逐条调用 `onAssign`，每次基于旧 `entries` 闭包计算，后一次覆盖前一次结果
- 修复：在 App.jsx 新增 `handleBatchAssign` 方法，在内存中链式执行所有 `performAssignment`，最后一次写入 state；WaitlistTable 和 ShowCards 的 drop handler 区分单条/多条调用对应方法
# 2026-05-25 工作日志

### 09:25  -  UI优化与美化

- 界面美化与交互优化
- 修复工号太长导致的布局溢出问题
- 更新原型文件UI为最新设计风格

### 11:27  -  部署与PRD整理

- 部署局域网环境供同事体验，访问地址 http://192.168.3.6:5173/
- 启动产品经理子代理重新梳理PRD
- 将PRD同步到飞书，命名为「演出候补递补管理系统PRD」

### 11:58  -  剧目删除后卡片消失Bug修复

- 问题：蓝色区域右键删除剧目后，右侧剧目卡片也一并消失
- 根因：`allShowCombinations` 纯从 `entries` 计算，候补记录删光后剧目从列表中消失
- 修复：让 `allShowCombinations` 合并 `deletedShows` 中的剧目，确保已删除剧目卡片始终可见（红色边框+半透明+已删除标签）

### 12:00  -  多选批量拖拽递补功能

- 候补名单表格工号前新增多选框，支持单选/全选
- 选中行高亮蓝色背景，拖拽选中行时携带所有选中记录
- 多条拖拽显示自定义拖拽图像「X 条记录」

### 12:04  -  批量递补只成功1条Bug修复

- 问题：拖拽10条记录到剧目卡片，只有1条成功递补
- 根因：循环中逐条调用 `onAssign`，每次基于旧 `entries` 闭包计算，后一次覆盖前一次结果
- 修复：在 App.jsx 新增 `handleBatchAssign` 方法，在内存中链式执行所有 `performAssignment`，最后一次写入 state；WaitlistTable 和 ShowCards 的 drop handler 区分单选/多条调用对应方法

### 12:35  -  deletedShows 持久化修复

- 问题：已删除的剧目在刷新页面或递补操作后消失（只存在 React state 中）
- 修复：storage.js 新增 loadDeletedShows / saveDeletedShows / clearDeletedShows，将 deletedShows 存入 LocalStorage；App.jsx 初始化时加载、删除时保存、清除数据时清空

### 12:39  -  多选计数浮动条

- 表格底部新增深色浮动条，显示「已选择 X 条记录」及操作提示
- 右侧有「清除选择」按钮

### 12:54~13:01  -  候补时间字段调整

- 导出 CSV 和表格列名：「递补时间」→「候补时间」，去掉递补时间列
- 导入时优先读取 CSV 原始的「候补时间」字段（entryData.候补时间），没有则 fallback 到候补创建时间
- 修复导出 CSV 候补时间为空的问题：导出 headers 用「候补时间」但数据 key 是「候补创建时间」，导致读不到值。导出映射改为 header=候补时间 → key=候补创建时间
