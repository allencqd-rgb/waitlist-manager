# 坦克大战移动端适配

## 目标
为坦克大战游戏添加移动端触控支持

## 改动
1. **虚拟手柄**：左下方向键(▲▼◀▶) + 右下射击按钮(🔥)，触屏设备自动显示
2. **响应式布局**：画布≤850px时等比缩放，UI面板/按钮尺寸适配
3. **触控行为**：禁止页面缩放、游戏中禁止触摸滚动、touchstart/end映射到keyboard keys
4. **viewport meta**：添加 maximum-scale=1.0, user-scalable=no

## 代码位置
- CSS: `@media` + `#mobileControls` + `.dpad-btn` + `#fireBtn` 样式
- HTML: `#mobileControls` div 含方向键和射击按钮
- JS: IIFE 绑定 touchstart/touchend 事件 + resizeCanvas 函数

## 部署
已推送到 https://github.com/allencqd-rgb/tank-battle (commit 9da47dc)
