# 新春观影活动页面 - Vue 3 项目

## Objective
将蓝湖设计稿（5种状态）还原为完整的 Vue 3 项目

## 技术栈
- Vue 3 + TypeScript
- Vite 6
- Vue Router 4（Hash 模式）
- TailwindCSS v4（通过 @tailwindcss/vite 插件）
- 纯 CSS 组件样式（scoped）

## 页面状态（对应设计稿5张图）

| 路由 | 状态 | 说明 |
|------|------|------|
| `/` | 信息填写 | 输入手机号，提交按钮 |
| `/` (mode=view) | 已填写 | 显示"更改信息"按钮 |
| `/closed` | 信息收集已截止 | 带截止提示banner |
| `/closed` (变体) | 截止+信息展示 | 大标题"信息收集已截止"+表单数据只读 |

## 核心功能
- 手机号格式验证（正则：1[3-9]\d{9}）
- 表单状态切换（edit ↔ view）
- 红色渐变背景 + 金色标题还原
- 毛玻璃效果卡片（backdrop-filter: blur）
- 移动端优先布局（max-width: 480px 居中）

## 文件结构
```
spring-festival-movie/
├── src/
│   ├── main.ts          # 入口，挂载 router
│   ├── App.vue          # 根组件，仅 <router-view />
│   ├── style.css        # TailwindCSS 导入 + 全局变量
│   ├── router/index.ts  # 3个路由
│   └── views/
│       ├── FormPage.vue       # 主页（填写/已填写双模式）
│       ├── SubmittedPage.vue  # 已提交页（更改信息）
│       └── ClosedPage.vue     # 已截止页
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 运行方式
```bash
cd spring-festival-movie
npm run dev
```

## 构建结果
✅ TypeScript 类型检查通过
✅ Vite 构建成功（158ms）
✅ 总产物 ~108KB（gzip ~40KB）

## 待优化（可选）
- 底部礼物插画用真实图片替换当前渐变占位
- 接入真实 API 替换模拟提交
- 增加 loading 动画和 toast 提示组件
